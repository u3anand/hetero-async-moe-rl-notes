# Lockfile — the tested vLLM (fork, commit) for B2. setup.sh installs the submodule under
# code/forks/vllm pinned to this commit. Update when you rebase the fork.
# Format: <name> <fork-remote> <commit|branch>
#
# B2 needs ONLY the vLLM fork (serving-only; no prime-rl / mini-swe-agent / udocker).
# The MoE-gate hook (vllm/model_executor/layers/expert_load_logger.py + the one-line call in
# models/olmoe.py, and the external-gate path in models/qwen3_moe.py) is kernel-free →
# VLLM_USE_PRECOMPILED=1, no CUDA build. Capture MUST run with --enforce-eager (the hook
# no-ops under torch.compile). The same insertion point is shared with B1-A2's router-logit
# hook. qwen3_moe also emits per-token prefill/decode tags + a sibling expert_mb.<pid>.jsonl
# with per-micro-batch imbalance (set VLLM_B2_SIM_GPUS for the round-robin EP layout).
#
# Off base commit f56ac5689; b2-expert-telemetry branches off b1-instrumentation (keeps the
# shared B1 router-logit hook). Submodule pointer is bumped to this commit.
vllm   git@github.com:u3anand/vllm.git   f7cd011d4   # b2-expert-telemetry: olmoe+qwen3 expert-load hook, per-token prefill/decode split (query_start_loc, vLLM V1), per-micro-batch imbalance (expert_mb.jsonl)
